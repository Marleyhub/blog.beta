{{#each erros}}
<div class="alert alert-danger">{{texto}}</div>
{{else}}

{{/each}}

<h3>Nova postagem:</h3>
<div class="card">
    <div class="card-body">
<form action="/admin/postagem/add" method="post">
<input type="hidden" name="id" value="{{Postagem._id}}">
<label for="name">Nome:</label> 
<input type="text" id="name" name="name" placeholder="Nome da postagem" class="form-control">
<label for="slug">Slug:</label>
<input type="text" id="slug" name="slug" placeholder="Slug da postagem" class="form-control">
<br>
<button type="submit" class="btn btn-success">Criar postagem</button>  
</form>
    </div>
</div>